CREATE TABLE Queue(
	ID				INT				PRIMARY KEY		IDENTITY(1, 1),
	PatientID		SMALLINT		FOREIGN KEY		REFERENCES PatientInformation(ID) ON DELETE CASCADE,
	BloodPressure	SMALLINT		NOT NULL,
	WeightKG		DECIMAL(10, 2)	NOT NULL,
	HeightFT		DECIMAL(10, 2)	NOT NULL,
	Reason			VARCHAR(7)		NOT NULL
);